{{>menu}}
<div class="container">
    <div class="row">
        <div class="col-6">
            <h1 class="fs-1 mb-5"><i class="fa-solid fa-boxes-packing"></i> Productos</h1>  
        </div>
    </div>
</div>

<div class="container">
    <div class="row">
        {{#each productos}}
            {{> producto
                frutVerd = this
            }}
        {{/each}}
    </div>
</div>

{{#if fruVerduras}}
    {{> productoBuscar
        buscarProducto = fruVerduras
    }}
{{/if}}